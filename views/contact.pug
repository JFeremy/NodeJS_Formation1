<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="css/sanitize.css")
    link(rel="stylesheet", href="css/style.css")
    title #{title}
  body
    section
      h1 Nous contacter
      p.flash #{flash}
      form(method="POST" action='/')
        label(for="name") Prénom :
        input#name(type="text" placeholder="Prénom" name="name" value="jean michel")
        label(for="email") Mail :
        input#email(type="mail" placeholder="Email" name="email" value="jean-michel@gmmail.com")
        label(for="message") Message :
        textarea#message(name="message", cols="30", rows="10") Salut mon pote !
        button(type="submit") Envoyer